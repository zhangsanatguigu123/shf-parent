<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.dao.CommuntyDao">
   <select id="findPage" resultType="community">
         select *  from hse_community
          <where>
                <if test="name!=null and name!=''">
                  and   name like concat('%',#{name},'%')
                </if>
              <if test="areaId!=null and areaId!='' ">
                and  area_id=#{areaId}
              </if>

              <if test="plateId!=null and plateId!='' ">
                 and plate_id=#{plateId}
              </if>
              and
              is_deleted=0
          </where>
   </select>

    <insert id="insert">
         insert into  hse_community(name, description, area_id, plate_id, address, build_years, property_price, property_company, developer, build_num, house_num, average_price)
         VALUES (#{name},#{description},#{areaId},#{plateId},#{address},#{buildYears},#{propertyPrice},#{propertyCompany},#{developer},#{buildNum},#{houseNum},#{averagePrice})
    </insert>

    <select id="findById" resultType="community">
         select * from hse_community where id=#{id}
    </select>
    <update id="update">
         update hse_community
             <set>
                    <if test="name!=null and name!=''">name=#{name},</if>
                    <if test="description!=null and description!=''">description=#{description},</if>
                    <if test="areaId!=null and areaId!=''">area_id=#{areaId},</if>
                    <if test="plateId!=null and plateId!=''">plate_id=#{plateId},</if>
                    <if test="address!=null and address!=''">address=#{address},</if>
                    <if test="buildYears!=null and buildYears!=''">build_years=#{buildYears},</if>
                    <if test="propertyPrice!=null and propertyPrice!=''">property_price=#{propertyPrice},</if>
                    <if test="propertyCompany!=null and propertyCompany!=''">property_company=#{propertyCompany},</if>
                    <if test="developer!=null and developer!=''">developer=#{developer},</if>
                    <if test="buildNum!=null and buildNum!=''">build_num=#{buildNum},</if>
                    <if test="houseNum!=null and houseNum!=''">house_num=#{houseNum},</if>
                    <if test="averagePrice!=null and averagePrice!=''">average_price=#{averagePrice},
                    </if>
                   update_time=now()
             </set>
        where id=#{id}
    </update>

    <update id="delete">
         update hse_community set is_deleted=1 where id=#{id}
    </update>
    <select id="findAll" resultType="community">
         select * from hse_community where is_deleted=0
    </select>
</mapper>